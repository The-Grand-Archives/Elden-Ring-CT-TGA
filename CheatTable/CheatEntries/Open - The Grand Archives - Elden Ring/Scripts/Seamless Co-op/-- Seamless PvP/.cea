{$lua}
if syntaxcheck then return end
[ENABLE]
if not getAddressSafe("elden_ring_seamless_coop.dll") and not getAddressSafe("ersc.dll") then
   local e = "The scripts in this header are only intended for the Seamless Co-op mod."
   ShowMessage(e)
   error(e,2)
end

[DISABLE]
