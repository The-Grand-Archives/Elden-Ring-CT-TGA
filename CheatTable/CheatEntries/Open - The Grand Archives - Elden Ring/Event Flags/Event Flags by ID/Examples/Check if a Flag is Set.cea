{$lua}
if syntaxcheck then return end

[ENABLE]
local id = 62000 -- the id of the flag to check

if ef.getFlag(id, "boolean") then
	print(id.." is set!")
else
	print(id.." is not set!")
end
disableMemrec(memrec)

[DISABLE]
