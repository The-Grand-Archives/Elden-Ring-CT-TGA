{$lua}
if syntaxcheck then return end
[ENABLE]
if equipItem == nil then
    getAddressList().getMemoryRecordByDescription("equipItem_code").active = true
    local e = "'equipItem' symbol was not found.\nTry enabling this script again."
    ShowMessage(e)
    error(e, 2)
end

-- Actual code is located in Misc/WIP -> Dependencies -> Global Functions

-- equipItem(slot, idx)
-- unequipItem(slot)

[DISABLE]
