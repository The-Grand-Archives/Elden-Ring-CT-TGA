#ifndef _PARAMS_H
#define _PARAMS_H

// This header is here for backwards compatibility so that scripts using
// #include "params.h" instead of #include <tga/params.h> are still functionnal.

#include "tga/params.h"

#endif